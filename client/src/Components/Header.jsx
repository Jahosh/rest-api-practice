import React from 'react';
import { PageHeader } from 'react-bootstrap';

const Header = () => {
  return (
    <PageHeader>API Practice <small>backed by express.js</small></PageHeader>
  );
};

export default Header;